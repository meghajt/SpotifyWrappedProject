from django.apps import AppConfig

class SpotifywrappedConfig(AppConfig):
    """
    Configuration class for the 'spotifywrapped' app.

    This class defines metadata and application-specific configurations
    for the Spotify Wrapped Django application.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "spotifywrapped"
